name: trackpad
description: "A remote Bluetooth trackpad for Windows and Mac."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  win32: ^6.0.0
  permission_handler: ^12.0.1
  shared_preferences: ^2.3.5
  nsd: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
